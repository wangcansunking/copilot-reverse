---
bump: patch
---
fix(setup): 1M-context [1m] suffix now follows each model's real context window for claude ids too, so newly-shipped 1M models like claude-opus-5 get Claude Code's 1M window instead of being sized at 200K. Previously the claude branch of `withClaude1mSuffix` ignored the fetched window and consulted a hardcoded list that omitted opus-5. Also added claude-opus-5 to the fallback 1M/model sets for the discovery-not-yet-resolved path.
